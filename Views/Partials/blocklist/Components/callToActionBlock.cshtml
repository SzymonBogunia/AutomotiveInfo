@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem<ContentModels.CallToActionBlock, ContentModels.BlockSettings>>
@using Umbraco.Extensions
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;

@{
    // Sprawdzenie ukrycia komponentu
    // SILNE TYPOWANIE USTAWIEŃ: Rzutujemy bezpiecznie za pomocą operatora 'as'
    var settings = Model.Settings as ContentModels.BlockSettings;

    // Jeśli rzutowanie przejdzie pomyślnie, sprawdzamy naszą flagę ukrycia
    if (settings?.HideBlockElement ?? false) { return; }

    // 1. Pobranie tekstów i tła
    var header = Model.Content.Header;
    var lead = Model.Content.Lead;

    var bgColor = Model.Content.BgColor?.Label ?? "bg-slate-900";

    // 2. Pobranie pierwszego linku z kolekcji
    var link = Model.Content.Link?.FirstOrDefault();
}

<div class="p-8 md:p-12 rounded-2xl border border-slate-800/80 @bgColor my-8 shadow-2xl">
    <div class="max-w-2xl">
        @if (!string.IsNullOrEmpty(header))
        {
            <h3 class="text-2xl md:text-3xl font-bold text-white mb-3">@header</h3>
        }

        @if (!string.IsNullOrEmpty(lead))
        {
            <p class="text-slate-300 mb-6 leading-relaxed">@lead</p>
        }

        @if (link != null)
        {
            <a href="@link.Url"
               target="@(!string.IsNullOrEmpty(link.Target) ? link.Target : "_self")"
               class="inline-flex items-center justify-center px-5 py-3 text-sm font-semibold rounded-lg bg-red-600 hover:bg-red-500 text-white transition-all hover:scale-[1.02]">
                @(string.IsNullOrEmpty(link.Name) ? "Kliknij tutaj" : link.Name)
            </a>
        }
    </div>
</div>